galaxy_info:
  author: Stan Chan
  description: Docker
  license: APLv2
  min_ansible_version: 2.8
  # min_ansible_container_version:
  # github_branch:

  platforms:
    - name: Alpine
      versions:
        - all
    - name: EL
      versions:
        - 8
        - 7
    - name: Debian
      versions:
        - 10
        - 9
    - name: Ubuntu
      versions:
        - bionic
        - xenial

  galaxy_tags:
    - docker
    - docker-compose

dependencies:
  - { role: pip }